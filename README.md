Faça o git clone para realizar o download do projeto.

No terminal execute o seguinte comando para instalar todos os nuggets necessários: Get-Content requirements.txt | ForEach-Object { dotnet add package $_ }
